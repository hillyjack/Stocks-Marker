
export class User {
  userId: number;
  userName: number;
  userPass: number;


  constructor(userId, userName, userPass) {
    this.userId = userId;
    this.userName = userName;
    this.userPass = userPass;
  }
}

